apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: shop-replicaset
  labels:
    app: shop
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shop
  template:
    metadata:
      labels:
        app: shop
    spec:
      containers:
      - name: shop
        image: hashicorp/http-echo
        args:
          - "-text=Go shopping"
        ports:
        - containerPort: 5678
