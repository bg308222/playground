apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: shop-statefulset-with-subdomain
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shop
  template:
    metadata:
      labels:
        app: shop
    spec:
      subdomain: shop-service-headless
      containers:
      - name: netshoot
        image: nicolaka/netshoot
        command: ["sleep", "infinity"]
        volumeMounts:
        - name: shop-storage
          mountPath: /data
  volumeClaimTemplates:
  - metadata:
      name: shop-storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi